import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};
module.exports = {
  productionBrowserSourceMaps: false, // در محیط dev هم می‌تواند مفید باشد
}
export default nextConfig;
